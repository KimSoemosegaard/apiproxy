#!/usr/bin/perl
##  ====================================================================
##  bin/@package@d
##  ====================================================================

##  Pragmas
use strict;
use warnings;
use open qw(:std :utf8);
use utf8;

##  Packages
use Plack::Runner;

##  Local packages
use lib '@prefix@/lib';
use @Package@::App;

##  Start the application
#Plack::Runner->new->run(@Package@::App->new(@ARGV));
my $runner = Plack::Runner->new;
#$runner->parse_options(qw(-E production));
$runner->run(@Package@::App->new(@ARGV));
